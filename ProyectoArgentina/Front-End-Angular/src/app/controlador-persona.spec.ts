import { ControladorPersona } from './controlador-persona';

describe('ControladorPersona', () => {
  it('should create an instance', () => {
    expect(new ControladorPersona()).toBeTruthy();
  });
});
